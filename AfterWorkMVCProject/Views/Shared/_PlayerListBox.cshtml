@model PlayerListBoxVM

    <div class="center">
        <h2>Digikalja</h2>
        <table class="table table-hover table-dark">
            <thead>
                <tr>
                    <th scope="col">@Model.Id</th>
                    <th scope="col">@Model.UserName</th>
                    <th scope="col">@Model.Score</th>
                </tr>
            </thead>
            <tbody id="userTable">
            </tbody>
        </table>
    </div>
